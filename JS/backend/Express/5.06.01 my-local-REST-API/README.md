# 🃏 Jokes API

واجهة برمجية (API) بسيطة لإدارة النكات باستخدام **Node.js + Express** مع التخزين في ملف JSON.

---

## 🚀 تشغيل المشروع

### المتطلبات
- Node.js (v16 أو أحدث)
- npm

### التثبيت
```bash
npm install
```

### تشغيل السيرفر
```bash
node index.js
```

📍 السيرفر سيعمل على:
```
http://localhost:3000
```

---

## 📁 بنية البيانات (JSON)

يتم تخزين البيانات في ملف `jokes.json` بالشكل التالي:

```json
{
  "metadata": {},
  "jokes": [
    {
      "id": 1,
      "joke": "نكتة مثال"
    }
  ],
  "statistics": {}
}
```

⚠️ جميع العمليات تعدّل فقط `jokes` بدون تخريب باقي الـ Object.

---

## 📌 Endpoints

### 🔹 GET /jokes
إرجاع جميع النكات.

### 🔹 GET /jokes/:id
إرجاع نكتة حسب المعرّف.

### 🔹 POST /jokes
إضافة نكتة جديدة.

**Body (JSON):**
```json
{
  "joke": "هذه نكتة جديدة 😄"
}
```

### 🔹 PUT /jokes/:id
تعديل نكتة كاملة.

### 🔹 PATCH /jokes/:id
تعديل جزئي لنكتة.

### 🔹 DELETE /jokes/:id
حذف نكتة.

### 🔹 GET /jokes/filter/:keyword
البحث داخل النكات.

---

## 🧪 الاختبار
استخدم Postman أو Thunder Client وتأكد من إرسال:
```
Content-Type: application/json
```

---

## ⚠️ ملاحظات
- المشروع لأغراض تعليمية
- لا يستخدم قاعدة بيانات
- غير مناسب للإنتاج

---

## 📜 الترخيص
مشروع تعليمي.
